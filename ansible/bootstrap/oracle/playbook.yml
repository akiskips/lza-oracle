---
- hosts: all
  gather_facts: true
  become: true
  tasks:
    - name: Show interpreter
      debug: var=ansible_python_interpreter

    - name: Get interpreter version
      command: "{{ ansible_python_interpreter }} --version"
  roles:
  - common
  #- grid
  - oracle19c  #oracle software installatio
  - dbca       # database creation
